spent like an hour making this just to find out rn-paper alreadhy have it...



buttonContainer: {
        flexDirection: 'column',
        justifyContent: 'space-around',
        position: 'absolute',
        top: 0,
        right: 4
    },
    floatingView: {
        position: 'absolute',
        top: 0,
        left: 0,
        height: Dimensions.get('window').height,
        width: '100%',
        borderWidth: 2,
        borderColor: "red"
    },
    imgButton: {
        flex: 1,
        alignItems: "center",
        margin: 4,
        paddingVertical: 1,
        paddingHorizontal: 24,
        borderWidth: 4,
        borderColor: colors.buttonColor,
        borderRadius: 6,
        backgroundColor: colors.buttonColor,
        
    },
    buttonText: {
        color: "white",
        textAlign: "center",
        fontSize: 14,
        fontWeight: "bold"
    },

    addTableMenu: {
        flexDirection: 'column',
        justifyContent: 'flex-end',
        position: 'absolute',
        bottom: 16,
        right: 16,
        height: (64*3)+8,
        width: '50%',
        borderColor: 'black',
        borderWidth: 4
    },
    addTable: {
        height: 64,
        width: 64,
        position: 'absolute',
        bottom: 0,
        right: 0,
        alignContent: 'center',
        borderWidth: 8,
        borderColor: colors.buttonColor,
        borderRadius: 60,
        backgroundColor: colors.buttonColor,
    },
    addButton: {
        textAlign: "center",
        fontSize: 32,
        color: "white"
    }



const [menuVisible, setMenuVisible] = React.useState(false);

const toggleMenu = () => setMenuVisible(!menuVisible)



{/* An absolute View the size of the window, used as reference for the floating button */}
        <View style={style.floatingView}>

            {/* an absolute positioned View for the + button and take/select picture button */}
            <View style={style.addTableMenu}>

                        {/* the choose image buttons, toggled with toggleMenu */}
                        {menuVisible ? (

                            <View style={style.buttonContainer}>

                                <Title style={style.title}>Choose Image:</Title>

                                <TouchableOpacity style={style.imgButton} onPress={() => console.log('Take Picture')}>
                                    <Text style={style.buttonText}>Take Picture</Text>
                                </TouchableOpacity>

                                <TouchableOpacity style={style.imgButton} onPress={() => console.log('Select Image')}>
                                    <Text style={style.buttonText}>Select Image</Text>

                                </TouchableOpacity>

                            </View>

                        ) : null}

                        {/* the floating + button */}
                        <TouchableOpacity style={style.addTable} onPress={() => toggleMenu()}>

                            <View >
                                <Text style={style.addButton}>+</Text>
                            </View>


                        </TouchableOpacity>
                            


                    </View>

        </View>